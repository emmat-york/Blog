import { ColorObserverDirective } from './color-observer.directive';

describe('ColorObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorObserverDirective();
    expect(directive).toBeTruthy();
  });
});
