export interface ArticleFormData {
    auther: string;
    header: string;
    article: string;
    articleReleaseDate: Date;
    id?: string;
}

export interface FbCreateResponse {
    name: string;
}
